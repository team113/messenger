import 'package:flutter/cupertino.dart';

class CustomSelection extends StatelessWidget {
  const CustomSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
